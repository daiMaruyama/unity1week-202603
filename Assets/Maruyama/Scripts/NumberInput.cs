using UnityEngine;
using Cysharp.Threading.Tasks;

public class NumberInput : MonoBehaviour
{
    public async UniTask<int> WaitForInput()
    {
        // ユーザーからの入力を待つロジック
        return 0; // 仮の返り値
    }
}
